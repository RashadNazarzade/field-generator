import type { ReservedKeys } from '../types/base.js';

export const RESERVED_KEYS: Set<ReservedKeys> = new Set([
  'key',
  'path',
  'elementAt',
  'at',
  'KEY',
  'PATH',
  'ELEMENT_AT',
  'AT',
]);
